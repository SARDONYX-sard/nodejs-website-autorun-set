# JavaScript CircleCI 2.0 configuration file.
# Check https://circleci.com/docs/2.0/language-javascript/ for more details.
version: 2.1
jobs:
  node_v12:
    working_directory: "~/repo"
    docker:
      - image: "cimg/node:12.22.0-browsers"
    steps:
      - "checkout"
      - restore_cache:
          key: 'node-12.22.0-browsers-node-modules-{{ checksum "package.json" }}'
      - run:
          name: "Install packages"
          command: "npm ci"
      - save_cache:
          key: 'node-12.22.0-browsers-node-modules-{{ checksum "package.json" }}'
          paths:
            - "node_modules"
      - run:
          command: chromedriver
          background: true
      - run:
          name: "Execute linters"
          command: "npm run lint"
      - run:
          name: "Execute tests"
          command: "npm test && npm run coverage"
  node_v10:
    working_directory: "~/repo"
    docker:
      - image: "cimg/node:10.24.0-browsers"
    steps:
      - "checkout"
      - restore_cache:
          key: 'node-10.24.0-browsers-node-modules-{{ checksum "package.json" }}'
      - run:
          name: "Install packages"
          command: "npm ci"
      - save_cache:
          key: 'node-10.24.0-browsers-node-modules-{{ checksum "package.json" }}'
          paths:
            - "node_modules"
      - run:
          name: "Execute linters"
          command: "npm run lint"
      - run:
          command: chromedriver
          background: true
      - run:
          name: "Execute tests"
          command: "npm test"
workflows:
  version: 2
  build:
    jobs:
      - "node_v12"
      - "node_v10"
